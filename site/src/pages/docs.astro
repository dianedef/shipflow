---
import BaseLayout from "../layouts/BaseLayout.astro";
import Footer from "../components/Footer.astro";
import NavBar from "../components/NavBar.astro";

const coreDocs = [
  ["AGENT.md", "Where a fresh agent should look first depending on the task."],
  ["CONTEXT.md", "How to orient quickly in the repo, identify hotspots, and route edits."],
  ["CONTEXT-FUNCTION-TREE.md", "How to navigate large procedural shell files without re-reading everything."],
  ["CONTENT_MAP.md", "Where content lives: docs, landing pages, semantic clusters, public surfaces, and repurposing destinations."],
  ["docs/editorial/README.md", "How public content, claims, page intent, and Astro content schema boundaries stay aligned."],
  ["docs/skill-launch-cheatsheet.md", "Markdown reference for master skills, supporting skills, and argument modes."],
  ["README.md", "Public overview, positioning, onboarding, and high-level framework story."],
  ["shipflow-spec-driven-workflow.md", "The execution doctrine behind sf-spec, sf-ready, sf-start, and sf-verify."]
];

const decisionDocs = [
  ["BUSINESS.md", "For whom the product exists, what value it creates, and what model it may support."],
  ["PRODUCT.md", "What the product does, which workflows matter, and which non-goals should stay out."],
  ["BRANDING.md", "How the product should sound, what it can claim, and how trust is earned."],
  ["GTM.md", "How the product is presented publicly, which objections matter, and what proof supports it."],
  ["ARCHITECTURE.md", "How the system is organized, where boundaries sit, and which invariants hold."],
  ["GUIDELINES.md", "How contributors should work inside the repo and which technical rules stay stable."]
];

const githubDocUrl = (name: string) => `https://github.com/dianedef/ShipFlow/blob/main/${name}`;
---

<BaseLayout
  title="ShipFlow Docs | Navigation and contracts"
  description="Documentation entrypoint for ShipFlow: context layer, workflow doctrine, and decision contracts."
>
  <main class="page-shell">
    <NavBar />

    <section class="section">
      <div class="section-heading">
        <p class="eyebrow">Docs Overview</p>
        <h1 class="page-title">The docs are built for fast agent navigation, not for encyclopedic sprawl.</h1>
        <p>
          ShipFlow documentation is structured so a fresh agent can find the
          right context, understand the decision surface, and execute against
          explicit contracts instead of guessing from chat history.
        </p>
      </div>
    </section>

    <section class="section">
      <div class="cta-panel">
        <p class="eyebrow">Public Skill Pages</p>
        <h2>The docs explain the contracts. The skills hub explains the workflow in public language.</h2>
        <p>
          If you are evaluating ShipFlow from the outside, start with the
          skills hub. It shows the concrete workflow moves behind the framework
          without requiring you to read internal execution contracts first.
        </p>
        <div class="hero-actions">
          <a class="button button-primary" href="/skills">Open the skills hub</a>
          <a class="button button-secondary" href="https://github.com/dianedef/ShipFlow/tree/main/skills">Browse skill files on GitHub</a>
        </div>
      </div>
    </section>

    <section class="section">
      <div class="doc-grid">
        <article class="doc-card">
          <h3>Skill launch cheatsheet</h3>
          <p>
            The public mode page now shows which master skill to launch first,
            which focused skills own narrower proof lanes, and which arguments
            switch a workflow instead of describing the task. The Markdown
            reference lives in <code>docs/skill-launch-cheatsheet.md</code>.
          </p>
          <a class="doc-action" href="/skill-modes">Open the cheatsheet</a>
        </article>

        <article class="doc-card">
          <h3>Questions that come up repeatedly</h3>
          <p>
            If you want the direct version first, the FAQ page covers the most
            common questions about documentation scope, skills, and framework
            behavior.
          </p>
          <a class="doc-action" href="/faq">Open the FAQ</a>
        </article>

        <article class="doc-card">
          <h3>Public content stays tied to product truth</h3>
          <p>
            ShipFlow separates the content map from the governance layer that
            checks public content, claims, page intent, and Astro content
            schema boundaries before public copy drifts from delivered behavior.
          </p>
          <a class="doc-action" href="https://github.com/dianedef/ShipFlow/tree/main/docs/editorial">
            Browse editorial governance
          </a>
        </article>

        <article class="doc-card">
          <h3>Specs are chantier registries</h3>
          <p>
            For spec-first work, the spec now carries the durable skill-run
            history: which lifecycle skills ran, which model was used, what
            result was recorded, and which command should happen next.
          </p>
          <a class="doc-action" href="https://github.com/dianedef/ShipFlow/blob/main/shipflow-spec-driven-workflow.md">
            Read the workflow doctrine
          </a>
        </article>

        <article class="doc-card">
          <h3>Direct fixes still leave bug memory</h3>
          <p>
            `sf-fix` can patch a local bug quickly, but the default is still a
            durable `BUG-ID`, compact `BUGS.md` pointer, and detailed
            `bugs/BUG-ID.md` dossier unless the issue is a narrow minor
            exception.
          </p>
          <a class="doc-action" href="/skills/sf-fix">Open sf-fix</a>
        </article>

        <article class="doc-card">
          <h3>Skills stay readable by agents</h3>
          <p>
            ShipFlow audits skill discovery metadata so the catalog remains
            usable by Codex and Claude Code. The public proof keeps the
            discovery budget visible instead of letting the skill catalog drift
            into unreadable prompt inventory.
          </p>
          <a class="doc-action" href="/faq">Read the compatibility FAQ</a>
        </article>
      </div>
    </section>

    <section class="section">
      <div class="section-heading">
        <p class="eyebrow">Core Navigation</p>
        <h2>Start here when you need orientation.</h2>
      </div>

      <div class="doc-grid">
        {
          coreDocs.map(([name, body]) => (
            <article class="doc-card">
              <h3>{name}</h3>
              <p>{body}</p>
              <a class="doc-action" href={githubDocUrl(name)}>
                Open on GitHub
              </a>
            </article>
          ))
        }
      </div>
    </section>

    <section class="section">
      <div class="section-heading">
        <p class="eyebrow">Decision Contracts</p>
        <h2>Each document has one explicit role.</h2>
        <p>
          Business, product, brand, GTM, architecture, and guidelines are kept
          separate on purpose. That separation avoids duplicate logic, stale
          context, and conflicting contracts.
        </p>
      </div>

      <div class="doc-grid">
        {
          decisionDocs.map(([name, body]) => (
            <article class="doc-card">
              <h3>{name}</h3>
              <p>{body}</p>
              <a class="doc-action" href={githubDocUrl(name)}>
                Open on GitHub
              </a>
            </article>
          ))
        }
      </div>
    </section>

    <section class="section">
      <div class="section-heading">
        <p class="eyebrow">Remote Agent Access</p>
        <h2>OAuth callbacks still need a local path when the agent runs on a server.</h2>
        <p>
          Hosted MCP providers can ask Codex to complete an OAuth flow. When
          Codex runs remotely, the browser callback still lands on local
          `127.0.0.1`. ShipFlow documents that boundary so operators understand
          why a temporary SSH callback tunnel is part of the login workflow.
        </p>
      </div>

      <div class="doc-grid">
        <article class="doc-card">
          <h3>The callback is local to the browser</h3>
          <p>
            The provider redirects the browser to a localhost callback URL. If
            Codex is running on a server, that localhost address is otherwise
            the wrong machine.
          </p>
        </article>

        <article class="doc-card">
          <h3>ShipFlow opens a temporary SSH path</h3>
          <p>
            The local tooling captures the fresh callback port, opens an SSH
            `-L` tunnel for that port, and closes it after the login flow. The
            app tunnels used for development remain separate.
          </p>
          <a class="doc-action" href="/remote-mcp-oauth-tunnel">Read the dedicated guide</a>
        </article>

        <article class="doc-card">
          <h3>Tokens stay with Codex and the provider</h3>
          <p>
            The tunnel does not replace OAuth and does not make ShipFlow a token
            store. It only gives the local browser a route back to the remote
            Codex process during authorization.
          </p>
        </article>
      </div>
    </section>

    <section class="section">
      <div class="section-heading">
        <p class="eyebrow">Pragmatic Tooling</p>
        <h2>ShipFlow chooses the runtime that creates the least friction in the real environments it touches.</h2>
        <p>
          For small tooling such as the metadata linter, the question is not
          which language looks the cleanest in isolation. The question is which
          runtime is most likely to already exist on dev machines, Linux
          servers, CI workers, and VPS targets.
        </p>
      </div>

      <div class="doc-grid">
        <article class="doc-card">
          <h3>Why Python is the pragmatic choice here</h3>
          <p>
            The linter uses the Python standard library only. That keeps it
            dependency-free, coherent with the repo, and more likely to run
            without extra setup than a Ruby-based equivalent on the targets
            ShipFlow cares about.
          </p>
        </article>

        <article class="doc-card">
          <h3>What “lighter” really means</h3>
          <p>
            Installing Ruby just for a linter is rarely lighter in practice.
            If ShipFlow ever needed an even smaller runtime surface, the real
            alternatives would be Bash for very simple rules, Node in a JS-heavy
            repo, or a Go binary for zero external runtime at a much higher
            implementation cost.
          </p>
        </article>
      </div>
    </section>

    <section class="section">
      <div class="section-heading">
        <p class="eyebrow">Tracking Discipline</p>
        <h2>The master tracker is not the local backlog.</h2>
        <p>
          ShipFlow treats tracking layers explicitly. It distinguishes the
          global multi-project tracker, the project section inside that master,
          and the local `TASKS.md` of the repository itself. Those levels are
          related, but they are not interchangeable.
        </p>
      </div>

      <div class="doc-grid">
        <article class="doc-card">
          <h3>Three levels, three meanings</h3>
          <p>
            {"${SHIPFLOW_DATA_DIR:-$HOME/shipflow_data}/TASKS.md"} is the master tracker for
            multiple projects. A project section inside that file is still part
            of the master view. `./TASKS.md` is the local operational tracker
            for one repository.
          </p>
        </article>

        <article class="doc-card">
          <h3>Historical done work is not active backlog</h3>
          <p>
            If the master contains historical completed work, that history can
            be imported as useful context. It should not be copied mechanically
            into a new local backlog as if it were still active work.
          </p>
        </article>

        <article class="doc-card">
          <h3>When a local TASKS.md is created later</h3>
          <p>
            ShipFlow re-reads the project history from the master, then
            separates active backlog from historical completed context. The
            local tracker should be cleaner and more project-shaped than the
            master source it was derived from.
          </p>
        </article>

        <article class="doc-card">
          <h3>The chantier history lives in the spec</h3>
          <p>
            `TASKS.md`, `AUDIT_LOG.md`, and `PROJECTS.md` remain operational
            trackers. A spec-first chantier records its own flow in the spec
            through `Skill Run History` and `Current Chantier Flow`.
          </p>
        </article>

        <article class="doc-card">
          <h3>Precision in language matters</h3>
          <p>
            ShipFlow should not say “the tracker did not exist” without naming
            which level it means: the master tracker, the project section inside
            it, or the local `TASKS.md` file.
          </p>
        </article>
      </div>
    </section>

    <section class="section">
      <div class="cta-panel">
        <p class="eyebrow">Execution Logic</p>
        <h2>Use the docs as product contracts, not as passive notes.</h2>
        <p>
          The context layer tells the agent where to start. The decision
          contracts tell it what matters. The workflow doctrine tells it how to
          move from ambiguity reduction to verified delivery.
        </p>
        <div class="hero-actions">
          <a class="button button-primary" href="https://github.com/dianedef/ShipFlow/blob/main/AGENT.md">Start with AGENT.md</a>
          <a class="button button-secondary" href="https://github.com/dianedef/ShipFlow/blob/main/shipflow-spec-driven-workflow.md">Read the workflow doctrine</a>
        </div>
      </div>
    </section>
  </main>
  <Footer />
</BaseLayout>
