interface:
  display_name: "tmux-capture-conversation"
  short_description: "Export tmux panes to Markdown files"
  default_prompt: "Use $tmux-capture-conversation to export the current tmux conversation to a Markdown transcript."
