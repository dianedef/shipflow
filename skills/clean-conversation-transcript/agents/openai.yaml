interface:
  display_name: "Clean Conversation Transcript"
  short_description: "Make tmux transcripts readable and reusable"
  default_prompt: "Use $clean-conversation-transcript to clean this exported tmux conversation Markdown file in place."
